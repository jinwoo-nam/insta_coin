import 'package:flutter/material.dart';

class InstaCoinArticleWidget extends StatelessWidget {
  const InstaCoinArticleWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
